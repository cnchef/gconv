# gconv 发布指南

本文档说明如何发布 gconv 的新版本。

---

## 📦 重要概念

**gconv 是 Go 模块（Library），不是可执行程序：**

- ✅ **不需要**编译二进制文件
- ✅ **不需要**打包 .exe 或 .tar.gz
- ✅ **只需要**推送 Git 标签（tag）
- ✅ 用户通过 `go get` 获取源码

---

## 🚀 发布流程（3 步完成）

### 第 1 步：准备发布

```powershell
cd D:\llm\gconv

# 1. 确保所有改动已提交
git status

# 2. 本地测试通过
go test -v ./...
go test -bench=. -benchmem

# 3. 代码格式化
go fmt ./...

# 4. 更新文档（如果需要）
# 编辑 README.md, README_US.md
```

---

### 第 2 步：创建版本标签

```powershell
# 语义化版本号：v主版本.次版本.修订号
# - v1.0.0: 第一个正式版本
# - v1.0.1: Bug 修复
# - v1.1.0: 新功能（向后兼容）
# - v2.0.0: 重大变更（不兼容）

# 简化版本（推荐）
git tag -a v1.0.0 -m "Release v1.0.0"

# 或详细版本
git tag -a v1.0.0 -m "Release v1.0.0

主要特性：
- 通用类型转换 Cast[T]
- 支持所有基础类型转换
- Struct 自动转换
- 98.2% 测试覆盖率
- 零第三方依赖
"

# 查看标签
git tag -l
git show v1.0.0
```

---

### 第 3 步：推送标签

```powershell
# 推送代码（如果有新提交）
git push origin main

# 推送标签（这会触发自动发布）
git push origin v1.0.0

# 或者推送所有标签
git push origin --tags
```

**完成！** 🎉

- ✅ GitHub Actions 自动创建 Release
- ✅ 自动生成更新日志
- ✅ pkg.go.dev 自动索引（约 24 小时内）

---

## 🔍 验证发布

### 1. 检查 GitHub Release

访问：https://github.com/cnchef/gconv/releases

应该看到新版本的 Release，包含：
- 版本号和标签
- 自动生成的更新日志
- 安装说明

### 2. 检查 pkg.go.dev（等待索引）

访问：https://pkg.go.dev/github.com/cnchef/gconv

**注意：** pkg.go.dev 需要一些时间（最多 24 小时）来索引新版本。

可以手动触发索引：
1. 访问 https://pkg.go.dev/github.com/cnchef/gconv@v1.0.0
2. 如果显示 404，点击 "Request" 按钮

### 3. 测试安装

在一个新项目中测试：

```powershell
# 创建测试项目
mkdir test-gconv
cd test-gconv
go mod init test

# 安装特定版本
go get github.com/cnchef/gconv@v1.0.0

# 创建测试文件
echo 'package main

import (
    "fmt"
    "github.com/cnchef/gconv"
)

func main() {
    result := gconv.Cast[int]("123")
    fmt.Printf("Result: %d\n", result)
}' > main.go

# 运行测试
go run main.go
```

---

## 📋 版本号规范（语义化版本）

### 格式：`v主版本.次版本.修订号`

| 版本类型 | 何时使用 | 示例 |
|---------|---------|------|
| **主版本** | 不兼容的 API 变更 | v1.0.0 → v2.0.0 |
| **次版本** | 添加向后兼容的新功能 | v1.0.0 → v1.1.0 |
| **修订号** | 向后兼容的 Bug 修复 | v1.0.0 → v1.0.1 |

### 预发布版本

```powershell
git tag v1.1.0-beta.1     # Beta 测试版
git tag v1.1.0-rc.1       # Release Candidate
git tag v1.1.0            # 正式版本
```

---

## 🔄 发布检查清单

发布前请确认：

- [ ] ✅ 所有测试通过
- [ ] ✅ 代码已格式化
- [ ] ✅ README 文档更新
- [ ] ✅ 没有未提交的改动
- [ ] ✅ 版本号符合语义化规范
- [ ] ✅ 标签信息清晰准确

---

## 🛠️ 常见操作

### 撤销错误的标签

```powershell
# 删除本地标签
git tag -d v1.0.0

# 删除远程标签
git push origin :refs/tags/v1.0.0
```

### 更新已发布的标签（不推荐）

```powershell
# 强制覆盖本地标签
git tag -f -a v1.0.0 -m "New message"

# 强制推送（谨慎使用！）
git push -f origin v1.0.0
```

**警告：** 不要修改已发布的标签，应该发布新版本！

### 查看所有版本

```powershell
# 查看本地标签
git tag -l

# 查看远程标签
git ls-remote --tags origin

# 按日期排序
git tag -l --sort=-version:refname
```

---

## 📊 发布后的工作

### 1. 宣传推广（可选）

- 在 Reddit r/golang 发布
- 在 Go 语言中文网分享
- 在 v2ex Go 节点发帖
- 撰写技术博客
- 更新项目 README 的徽章

### 2. 监控反馈

- 关注 GitHub Issues
- 查看 pkg.go.dev 的文档是否正确
- 收集用户反馈

### 3. 规划下一版本

- 记录待实现的功能
- 收集用户建议
- 修复发现的 Bug

---

## 🎯 自动化发布流程

推送标签后，GitHub Actions 会自动：

1. ✅ 运行所有测试
2. ✅ 生成版本更新日志
3. ✅ 创建 GitHub Release
4. ✅ 验证模块可以正常安装
5. ✅ 测试基本功能

查看发布状态：
https://github.com/cnchef/gconv/actions

---

## 📝 版本历史示例

```
v1.0.0 - 2025-11-27
  - Initial release
  - Universal type conversion
  - 98.2% test coverage

v1.0.1 - 2025-12-01
  - Fix: Handle nil values correctly
  - Docs: Update README examples

v1.1.0 - 2025-12-15
  - Feature: Add time.Time conversion
  - Feature: Add custom converter support
  - Improve: Better error handling

v2.0.0 - 2026-01-01
  - Breaking: Change Cast[T] signature
  - Feature: Major performance improvements
```

---

## 🆘 遇到问题？

### pkg.go.dev 没有索引新版本

**解决方法：**
1. 访问 https://pkg.go.dev/github.com/cnchef/gconv@v1.0.0
2. 点击页面上的 "Request" 按钮
3. 等待几分钟

### 用户无法下载新版本

**检查：**
1. 标签是否推送成功？`git ls-remote --tags origin`
2. 标签格式是否正确？必须是 `v1.0.0` 格式
3. go.mod 文件是否存在？

### Release 创建失败

**检查：**
1. GitHub Actions 日志
2. 权限设置（需要 `contents: write`）
3. 标签格式是否匹配 `v*.*.*`

---

## 📚 参考资料

- [Go 模块版本控制](https://go.dev/doc/modules/version-numbers)
- [语义化版本规范](https://semver.org/lang/zh-CN/)
- [pkg.go.dev 文档](https://go.dev/about/pkgsite)
- [GitHub Actions 文档](https://docs.github.com/actions)

---

**祝发布顺利！** 🚀

